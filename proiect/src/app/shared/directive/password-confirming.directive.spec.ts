import { PasswordConfirmingDirective } from './password-confirming.directive';

describe('PasswordConfirmingDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordConfirmingDirective();
    expect(directive).toBeTruthy();
  });
});
