import { EmailConfirmingDirective } from './email-confirming.directive';

describe('EmailConfirmingDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailConfirmingDirective();
    expect(directive).toBeTruthy();
  });
});
